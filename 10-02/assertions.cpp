#include <iostream>
#include <cstdlib> // now I can use atoi
#include <cassert> // now I can use assert!
using namespace std;

int main(int argc, char* argv[]) {

    assert(argc == 2);

    return 0;
}